<!DOCTYPE html>
</html>
    <head></head>
    <body>

        <h1>Logged-in as {{g.user.username}}</h1>
            <div class="side_panel">
                <ul>
                    <li><a href="#"><i class="projects">Daashboard</i></a></li>
                    <li><a href="#"><i class="projects">Projects</i></a></li>
                    <li><a href="#"><i class="projects">Manage</i></a></li>
                    <li><a href="#"><i class="projects">PlaceHolder#3</i></a></li>
                </ul>
            </div>

            <div class="Projects_assigned">
                <ul>
                    {% for project in projects %}
                    <form method="POST">
                            <h3>{{project.project_name}}</h3>
                            <li>Start date: {{project.start_date}} Deadline: {{project.deadline}} Phase: {{project.phase}}</li>
                            <div class="add_new_task">
                            <input type="hidden" id="ph" name="ph" value={{project.project_name}} />
                            <input type="text" name="add_task" placeholder="add new task"/>
                            <input type="submit" value="add task"/>
                            </div>
                                <ul>
                                    {% for task in project.tasks %}
                                        <li>{{task}}</li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </form>
                    {% endfor %}
                </ul>
            </div>
    </body>
</html>